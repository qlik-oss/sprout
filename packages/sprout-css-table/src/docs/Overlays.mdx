import { Canvas, Controls, Meta } from "@storybook/addon-docs/blocks";
import * as ListStories from "./ListTable.stories";
import * as DataStories from "./DataTable.stories";

<Meta title="Docs/Overlays" />

## loading

If the data are fecthed but you have the column definition you can display the Table using the `loading` property.

<Canvas of={DataStories.loading} />

Loading leverage the `renderCellLoading` if defined in the column definition.

```tsx
{
    field: "name",
    headerName: "Name",
    flex: 1,
    renderCellLoading: () => (
      <div className={classNames("flex", "row", "gap_m")}>
        <Skeleton variant="text" />
        <Skeleton variant="text" />
      </div>
    ),
  },
```

## noRowsOverlay

If no rows are passed you will have a default message. You can customize this overlay by using the property `noRowsOverlay`

<Canvas of={DataStories.NoRows} sourceState="shown" />

<Canvas of={DataStories.NoRowsOverlay} sourceState="shown" />
