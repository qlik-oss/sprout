import { Canvas, Controls, Meta } from "@storybook/addon-docs/blocks";

import * as Stories from "./canvas.stories";

<Meta title="Integration/canvas" />

[canvas](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/canvas) The Graphics Canvas HTML element.

It is a powerfull tool to create custom rendering on the web.

You can find here a demo of the current integration of canvas with Sprout.

<Canvas of={Stories.Canvas} />
